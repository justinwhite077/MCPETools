<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vgs.mctools"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk 
        android:minSdkVersion="11"
        android:targetSdkVersion="17" />

    <application
        android:icon="@drawable/iconx"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".MainActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
            <activity
            android:label="Mods Available"
            android:name=".modsAct" />
<activity
            android:label="Texture Packs Available"
            android:name=".tpAct" />
<activity
            android:label="Plugins PocketMine-MP Available"
            android:name=".pmpAct" />
<activity
            android:label="Others Tools &amp; Hacks Available"
            android:name=".otherAct" />
    </application>

</manifest>
